#########################
Day 14 - 22nd June 2026
#########################


	- Integrate Maven on Jenkins CICD process::
	
	
	- Jenkins - Build Orchestration Tool :::
	
	- Jenkins Architecture 
	
	- Setup Jenkins Architecture
	
	- Working Jenkins Scripts 		# Groovy Scripts
	
	
#########################
Day 15 - 23rd June 2026
#########################
	
	- Jenkins - Build Orchestration Tool :::
	
		- Is an open-source build orchestration Tool 
		- Used to Create the end-to-end CICD Pipeline to automation application builds and deployments
		- Used to perform any scheduled process automation 
	
	- Jenkins Architecture :: CI & CD 
	
		- Jenkins is based on master/Controller & slave/Node
		
			- Jenkins Master/Controller (VM) :		
								- Install Jenkins 
								- To Create Jenkins Jobs/Projects/Pipelines and scheduled to run on the Jenkins Slave Nodes 
		
				- Jenkins Slave_Node (VM) (Build Server):	
								- Install the Build Tools 
								- To Execute the Jobs  

	Alternative to Jenkins :
	
		- GitHub-Actions 
		- gitlab-ci 
		- Bamboo
		- Azure Pipelines 
		- AWS Code Pipelines 
	
	- Setup Jenkins Architecture :::

	Scenario1 :	Application Build and Release frequency is very less.
	
		Jenkins_Master (VM) 	
				==> Create Jenkins CI/CD Pipeline Projects & Execute the Build in same Jenkins_Master Node.		
		
		
		
	Scenario2 : Multiple Applications with high Build and Release frequencies.
		
		Jenkins_Master (VM)			
				==> To Create Jenkins CI/CD Pipeline Projects and schedule to the builds in the slave_nodes
																
			Jenkins_SlaveNode1 (VM)	==> Perform Application Builds - Java Applications 
			Jenkins_SlaveNode2 (VM)	==> Perform Application Builds - .Net Applications			
			Jenkins_SlaveNode3 (VM)	==> Perform Application Builds - Python Applications 
			Jenkins_SlaveNode4 (VM)	==> Perform Application Builds - Angular/NodeJS
			Jenkins_SlaveNode5 (VM)	==> Perform Application Builds - Ruby 		


	- Jenkins Pipelines/Jobs/Projects Workflow :
		
				- Application Build 
					==> Process of Compiling the Source Code, 
						Create the Artifacts(Executable code - Binaries - *.war/*.jar/*.exec/*.dll) 
				- Unit Test 
				- Promote the Changes to Test Environments
				- Notify the Testing Team thru Emails	
				
	CI/CD Workflow :::
	
		SCM_Checkout 
		Application Build 
		Unit Testing 
		Create Artifacts
		Promote the Application to Test Environments
		Notify 
		Deploy to Prod 	


	- Developers' Perspectives :
	
		- Developers are just the consumers of Jenkins using Jenkins Tools and Services.
		- Developers use Jenkins to execute/cancel/view the output - workspace of the Jenkins projects/Pipelines/Jobs.

	- DevOps Perspectives:
		
		- DevOps Team is the owner of Jenkins.		
		
	
		- Roles & Responsibilities of DevOps Team/Resources :
		
		- Jenkins Administration :
		
			- Installation & Configuration of Jenkins 
			- Jenkins Global System Configuration 
			- Plugins Management 
			- Tools Management 
			- User Management
			- Security Management 
			- Credentials Management  
			- Create Jenkins Job/Project in Jenkins Master
			- Create the Jenkins Master & Slave Node Configurations
			- Create CICD Pipeline Projects to automate the Applications build  and deployments
			- Onboard Applications to DevOps Practice
			- Periodic Backup & Recovery
			- Periodic Upgrade of Jenkins Services and Plugins 
			- Troubleshoot any Jenkins related Issues
			

	Implementations :
	
		- Installation & Configuration of Jenkins :::
		
		
	Package Management :
	
		To Install Jenkins Package : 
									# https://www.jenkins.io/doc/book/installing/
									# https://www.jenkins.io/doc/book/installing/linux/
		
			Type: Jenkins is a service package - it runs in the default port: 8080.
			
		
			1. Install Pre-Requisites					# JDK 
			
			2. Install Jenkins 							# Jenkins 
			
			3. Post Installation Activities				# Setup 
			
		
	- Jenkins Master/Controller (VM) :		Tools: jdk, jenkins, git
	
					- To Create Jenkins Jobs/Projects/Pipelines and scheduled to run on the Jenkins Slave Nodes 
	

	- Jenkins Global System Configuration :::
			# Define default Jenkins properties and connection to external servers 

	- Tools Management :::
			# Manage the external tools and its installation path.
			
			
Next :::
			
	- Plugins Management 
	- Tools Management 
	- User Management
	- Security Management 
	- Credentials Management  
	- Create Jenkins Job/Project in Jenkins Master
	- Create the Jenkins Master & Slave Node Configurations
	- Create CICD Pipeline Projects to automate the Applications build  and deployments
	- Onboard Applications to DevOps Practice
	- Periodic Backup & Recovery
	- Periodic Upgrade of Jenkins Services and Plugins 
	- Troubleshoot any Jenkins related Issues




#########################
Day 16 - 26th June 2026
#########################

	- Plugins Management :::
	
		- Install/Uninstall/Upgrade/Downgrade/Enable/Disable the plugins
	
	
		Jenkins_Master (VM)	--> jdk, jenkins, plugins(maven)		
				==> To Create Jenkins CI/CD Pipeline Projects and schedule to the builds in the slave_nodes
																
			Jenkins_SlaveNode1 (VM)	==> Perform Application Builds - Java Applications -->  jdk17, mvn3.5
			Jenkins_SlaveNode2 (VM)	==> Perform Application Builds - Java Applications -->  jdk21, mvn3.5.7			
			
			
	- User Management :::
	
		- Used to define the Users' Authentication	
		- Create/Update/Remove Users 
	
	- Security Management :::

		- Used to define the Users' Authorization
		- Allow/Deny Access
	
	- Credentials Management  :::
	
		- To maintain the credentials.
	
		
	
	- Create Jenkins Job/Project in Jenkins Master :
	
	
		- Jenkins Project/Jobs:
		
			- Free Style Project 	-- Based on manual configurations using GUI Console 
			
			
		- Variables :

			- Environment Variables :
			
				- Reserved Jenkins Keywords  - gets updated at every job execution
				
				- echo "jenkins-${JOB_NAME}-${BUILD_NUMBER}"
				
						var1 ==> 'xyz'
						echo "${var1}"
					
					Envi = 'QA' 
				
				
			- User defined Variables :::
			
Next:::
					
	- Pipeline Project 		-- Based on the Scripts - groovy Scripts		
							-- To Create CICD Pipelines and Onboard applications
	- Create the Jenkins Master & Slave Node Configurations
	- Create CICD Pipeline Projects to automate the Applications build  and deployments
	- Onboard Applications to DevOps Practice
	- Periodic Backup & Recovery
	- Periodic Upgrade of Jenkins Services and Plugins 
	- Troubleshoot any Jenkins related Issues									
									



#########################
Day 17 - 29th June 2026
#########################

	
	- Pipeline Project 		-- Based on the Scripts - groovy Scripts		
							-- To Create CICD Pipelines and Onboard applications
							
		- Jenkins Pipelines:

			- Scripted Pipelines:	
			
				- Typical Groovy Scripts 
				
			
			- Declarative Pipelines:

				- Snippet Generators 
				
					- Using input parameters the scripts can be generated.
							
pipeline {
    agent any

    stages {
        stage('Hello1') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Hello2') {
            steps {
                echo 'Hello Everyone'
            }
        }
    }
}


pipeline {
    agent any

    stages {
        stage('SCM_Checkout') {
            steps {
                echo 'Perform SCM Checkout for Java Application'
            }
        }
        stage('Application Build') {
            steps {
                echo 'Perform Java Application Build'
            }
        }
        stage('Deploy to QA Server') {
            steps {
                echo 'Deploy to QA Server'
            }
        }
    }
}							
							
	CI/CD Workflow :::
	
		SCM_Checkout 
		Application Build 
		Unit Testing 
		Create Artifacts
		Promote the Application to Test Environments
		Notify 
		Deploy to Prod 	

		
							
	- Create the Jenkins Master & Slave Node Configurations :::
	
	
	Setup:
	
		- Jenkins Master:(VM)
				==> To Create Jenkins CI/CD Pipeline Projects and schedule to the builds in the slave_nodes
				==> Tools:
						- git 
						- jdk
						- jenkins

			- Jenkins Slave Node:(VM)
				==> Perform Application Build for Java Maven Applications
				==>	Tools:
						- git 
						- jdk
						- Maven 
						
						
		- Setup the SSH Connection:
		
			- On Slave Node - Create linux user, and ssh keys.
			
		- Login to Jenkins Console -> Setup the Jenkins Node Configurations.
			
Create Jenkins Pipeline:

pipeline {
    agent { label 'slave1' }
    
    environment {
        JAVA_HOME = '/usr/lib/jvm/java-17-openjdk-amd64'
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    } 
    
    stages {
        stage('SCM_Checkout') {
            steps {
                echo 'Perform SCM Checkout for Java Application'
				git 'https://github.com/PL-DevOps-Cloud-2705/java-webapp-project.git'
            }
        }
        stage('Application Build') {
            steps {
                echo 'Perform Java Application Build'
				sh 'mvn clean package'
            }
        }
        stage('Deploy to QA Server') {
            steps {
                echo 'Deploy to QA Server'
            }
        }
    }
}
						
	
		
Next: 

	Setup the Target Tomcat Server :::
	
	- Web Application Server:
	
	- Tomcat WAS 
	- Publish Over SSH Plugins to Deploy the Artifacts to Target Server.
	- Create CICD Pipeline Projects to automate the Applications build  and deployments
	- Onboard Applications to DevOps Practice
	- Periodic Backup & Recovery
	- Periodic Upgrade of Jenkins Services and Plugins 
	- Troubleshoot any Jenkins related Issues





#########################
Day 18 - 30th June 2026
#########################

	- Jenkins_Master 			jdk_21
		- Jenkins_Slave 		jdk_21
		
		
	- Application Build :
		- https://github.com/PL-DevOps-Cloud-2705/java-webapp-project.git			# jdk17
		
		- jdk version 17
				
				
		- https://github.com/PL-DevOps-Cloud-2705/java-webapp-project2.git			# jdk21



	Environments:
	
		Dev 
		
		Build 					# *.war
		
		Targets: 
		
			Test 
				- QA 				*.war
				
				- UAT 				*.war
				
			Prod 
				- Prod_Servers		*.war
			

		

	Setup the Target Tomcat Server :::
	
	- Jenkins_Master (VM)
		- Jenkins_Slave (VM)

	- Test_Server (VM)
			--> Perform QA Testing 
			
			--> Tools :
					- jdk 
					- Tomcat WAS - port: 8080
		

	Deployment:::	Copying the artifacts from one server to another!
	
	
		Source_Source :														Target_Server:
			
			Jenkin_Slave 														Tomcat_Server 
			
				Source_File:					============>						Target_Directory:				
				
					*/target/*.war														/opt/tomcat/webapps
				
				Upon Deployment :
											
																						/opt/tomcat/webapps/*.war
																						
																						
																					/opt/tomcat/webapps/target/*.war
			
			
			
pipeline {
    agent { label 'slave1' }
    
    environment {
        JAVA_HOME = '/usr/lib/jvm/java-17-openjdk-amd64'
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    } 
    
    stages {
        stage('SCM_Checkout') {
            steps {
                echo 'Perform SCM Checkout for Java Application'
				git 'https://github.com/PL-DevOps-Cloud-2705/java-webapp-project.git'
            }
        }
        stage('Application Build') {
            steps {
                echo 'Perform Java Application Build'
				sh 'mvn clean package'
            }
        }
        stage('Deploy to QA Server') {
            steps {
              script{
				sshPublisher(publishers: [sshPublisherDesc(configName: 'Tomcat_Server_QA', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '.', remoteDirectorySDF: false, removePrefix: 'target/', sourceFiles: 'target/*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
			  }
            }
        }
    }
}			
			
			
			
			
			
			
			
			
			
			
	
sshPublisher(publishers: [sshPublisherDesc(configName: 'Tomcat_Server_QA', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '.', remoteDirectorySDF: false, removePrefix: 'target/', sourceFiles: 'target/*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])		

	
	
	- Web Application Server:
	
	- Tomcat WAS 
	- Publish Over SSH Plugins to Deploy the Artifacts to Target Server.
	- Create CICD Pipeline Projects to automate the Applications build  and deployments
	- Onboard Applications to DevOps Practice




Example ::::

pipeline {
    agent { label 'slave1' }
    
    environment {
        JAVA_HOME = '/usr/lib/jvm/java-17-openjdk-amd64'
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    } 
    
    stages {
        stage('SCM_Checkout') {
            steps {
                echo 'Perform SCM Checkout for Java Application'
				git 'https://github.com/PL-DevOps-Cloud-2705/java-webapp-project.git'
            }
        }
        stage('Application Build') {
            steps {
                echo 'Perform Java Application Build'
				sh 'mvn clean package'
            }
        }
        stage('Deploy to QA Server') {
            steps {
              script{
				sshPublisher(publishers: [sshPublisherDesc(configName: 'Tomcat_Server_QA', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '.', remoteDirectorySDF: false, removePrefix: 'target/', sourceFiles: 'target/*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
			  }
            }
        }
        stage('Peform QA Testing') {
            steps {
                echo 'Perform QA Testing'
            }
        }
        stage('Deploy to UAT Server') {
            steps {
              script{
				sshPublisher(publishers: [sshPublisherDesc(configName: 'Tomcat_Server_UAT', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '.', remoteDirectorySDF: false, removePrefix: 'target/', sourceFiles: 'target/*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
			  }
            }
        }
        stage('Peform UAT Testing') {
            steps {
                echo 'Perform UAT Testing'
            }
        }
        stage('Deploy to PROD Server') {
            steps {
              script{
				sshPublisher(publishers: [sshPublisherDesc(configName: 'Tomcat_Server_PROD', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '.', remoteDirectorySDF: false, removePrefix: 'target/', sourceFiles: 'target/*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
			  }
            }
        }
    }
}			

Next:

	- Enable Build Trigger 
	- Periodic Backup & Recovery
	- Periodic Upgrade of Jenkins Services and Plugins 
	- Troubleshoot any Jenkins related Issues



#########################
Day 19 - 2nd July 2026
#########################	

	- Jenkins Build Triggers :::	
	

		- Build Periodic :::		# https://crontab.guru/
			
			- Used to trigger the jenkins jobs irrespective of code change. - It is not meant for CI/CD.
			- To perform any process automation
			- The build is scheduled based on the crontab 


			- Environment Management:
			
					Non-Prod 						Prod 
					- Dev							- Prod Server
					- Build 
					- QA 
					- UAT 
				
				8AM - 10PM 
				
				
			- Jenkins_Start_Jobs	--> Start the Servers 
										Scheduled for 7:45AM
										
			- Jenkins_Stop_Jobs		--> Stop the Servers 
									-->	Scheduled for 10:15PM			
			
			
			
		- GitHub Webhook :::    meant for CI/CD
		
			- Used to trigger the jenkins Job whenever there is any commit happened in the remote repository
			- github webhook has be configured in the remote repository by using the jenkins URL


			GitHub Webhook - Payload URL: http://13.207.41.22:8080/github-webhook/
		
		
		
		- PollSCM :::

			- It is used to trigger the build based on the crontab, only if there is any changes happened in the source-code repository.
			
			
			Test Cycle:
			
				QA Testing Team:
				
					- 8, 12, 3
			
			
	
	- Email notifications :::
	
	
- Email ::::

	- GMail Account :
	
		- Two Way Authentication on GMail Account


Before Creating the EMail Scripts:
Configure Email Server:

To Setup the Email Notifications, First we need to set the Email Server in the Jenkins System configuration :




Login to Gmail :::

Click Manage Account Settings

@Left side panel - select Security 

MFA/Two-Way Authentication --> mobile#

Search for App password in the same page

Name:
Mail

What ? window
Which Appln - email	

App Password = 16 bytes of App Password. vaufxwhdbtnlltzr vauf xwhd btnl ltzr



How to setup the Email Server in the Jenkins Config:

goto manage jenkins -> system configuration 

goto Email Notification 
		
SMTP Server : smtp.gmail.com	

SMTP Authentication
	User Name: Enter Email ID 
	Password : 16 bytes of App Token (Created in Gmail)
	
Choose SSL :

SMTP Port :: 465
	

Email Notifications can be sent during:

- successful
- failure 
- abort
			
pipeline {
    agent { label 'slave1' }
    
    environment {
        JAVA_HOME = '/usr/lib/jvm/java-17-openjdk-amd64'
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    } 
    
    stages {
        stage('SCM_Checkout') {
            steps {
                echo 'Perform SCM Checkout for Java Application'
				git 'https://github.com/PL-DevOps-Cloud-2705/java-webapp-project.git'
            }
            post {
                failure {
                  sh "echo 'Send mail on failure'"					
					mail bcc: 's1.gmail.com', body: '"Jenkins Jobs Status ${BUILD_URL}"', cc: 's1.gmail.com', from: '', replyTo: '', subject: 'SCM-Checkout Failed - "Job '${JOB_NAME}' (${BUILD_NUMBER})"', to: 's1.gmail.com'
                }
              }	
        }
        stage('Application Build') {
            steps {
                echo 'Perform Java Application Build'
				sh 'mvn clean package'
            }
            post {
                failure {
                  sh "echo 'Send mail on failure'"					
					mail bcc: 's1.gmail.com', body: '"Jenkins Jobs Status ${BUILD_URL}"', cc: 's1.gmail.com', from: '', replyTo: '', subject: 'App Build Failed - "Job '${JOB_NAME}' (${BUILD_NUMBER})"', to: 's1.gmail.com'
                }
              }
        }
        stage('Deploy to QA Server') {
            steps {
              script{
				sshPublisher(publishers: [sshPublisherDesc(configName: 'Tomcat_Server_QA', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '.', remoteDirectorySDF: false, removePrefix: 'target/', sourceFiles: 'target/*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
			  }
            }
            post {
                Success {
                  sh "echo 'Send mail on Success-deployment'"						
					mail bcc: 's1.gmail.com', body: '"Jenkins Jobs Status ${BUILD_URL}"', cc: 's1.gmail.com', from: '', replyTo: '', subject: 'Deployment Successful', to: 's1.gmail.com'
                }
                Failure {
                  sh "echo 'Send mail on deployment Failure'"					
					mail bcc: 's1.gmail.com', body: '"Jenkins Jobs Status ${BUILD_URL}"', cc: 's1.gmail.com', from: '', replyTo: '', subject: 'Deployment Failed', to: 's1.gmail.com'
                }
                abort {
                  sh "echo 'Send mail on deployment Aborted'"					
					mail bcc: 's1.gmail.com', body: '"Jenkins Jobs Status ${BUILD_URL}"', cc: 's1.gmail.com', from: '', replyTo: '', subject: 'Deployment Aborted', to: 's1.gmail.com'
                }		
              }
        }
    }
}			
			
	
	
	- Periodic Backup & Recovery
	
		-> /var/lib/jenkins # Jenkins Home Directory
		
		
		
		
	- Periodic Upgrade of Jenkins Services and Plugins 
	
		->	--> v2.555.3	at least once in 3 months --> 6 months 
		
							--> After 2 years 
							
							v2.555.3 ===> 		v2.699.9  ==> v3.000.0	

							
							v3.000.0		
		
	- Troubleshoot any Jenkins related Issues	
	
	

Next:


	- Containerization using Docker :

